---
to: src/components/<%= dir %>/<%= h.changeCase.pascal(name) %>/components/<%= h.changeCase.pascal(name) %>.tsx
---

interface Props {
  
}

const <%= h.changeCase.pascal(name) %> = (props: Props) => {
  return (
    <></>
  )
}

export default <%= h.changeCase.pascal(name) %>;